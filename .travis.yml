# build reStructuredText documentation
---
language: python
python:
  - "2.7"
install: "pip install -q -t simp_5_1_0 -r requirements.txt --use-mirrors"
script: sphinx-build -n -b html -d _build/sphinx_cache docs/ _build/html
