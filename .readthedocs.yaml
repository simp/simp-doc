# Read the Docs build configuration file
# ------------------------------------------------------------------------------
# v2 config reference: https://docs.readthedocs.io/en/stable/config-file/v2.html
# ------------------------------------------------------------------------------

version: 2

formats: all

build:
  os: ubuntu-24.04
  tools:
    python: '3.13'

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
